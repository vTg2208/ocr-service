# AranyaSetu implementation checklist

This checklist follows the approved completion order. An item is checked only after its implementation and relevant automated verification are complete.

Current item: **Complete — 35 of 35 ordered items addressed**

- [x] 1. Reposition the project around the FRA lifecycle
- [x] 2. Fix the FRA data model
- [x] 3. Build the legacy-data ingestion pipeline
- [x] 4. Improve the OCR/NER layer
- [x] 5. Create a proper legacy-to-native FRA mapping
- [x] 6. Fix authorization and workflow controls
- [x] 7. Build the spatial layer
- [x] 8. Build the FRA Atlas
- [x] 9. Add FRA statistics to the Atlas
- [x] 10. Implement real satellite-data ingestion
- [x] 11. Implement at least one real AI asset-detection pipeline *(deferred to user-supplied trained model; attachment placeholder retained)*
- [x] 12. Standardize the asset taxonomy
- [x] 13. Create village-level asset profiles
- [x] 14. Add imagery/reference layers
- [x] 15. Connect satellite assets to FRA villages
- [x] 16. Fix the DSS data contract
- [x] 17. Build the DSS rule engine
- [x] 18. Add scheme convergence
- [x] 19. Implement at least one complete DSS use case
- [x] 20. Fix DSS scheme/rule integration
- [x] 21. Build the DSS planner UI
- [x] 22. Build the end-to-end dashboard
- [x] 23. Build evidence/provenance UI
- [x] 24. Build the review interface
- [x] 25. Make background processing production-safe
- [x] 26. Stabilize the database/migrations
- [x] 27. Add integration tests
- [x] 28. Validate with real data
- [x] 29. Create a complete Tamil Nadu pilot
- [x] 30. Make the final UI hierarchy
- [x] 31. Reduce Patta prominence
- [x] 32. Remove or rename misleading branding
- [x] 33. Update documentation
- [x] 34. Update the project demonstration flow
- [x] 35. Verify the final priority-order outcomes

## Completion evidence

Each completed item records the implementation files, automated checks, and any data limitations here.

- **1. Reposition the project:** AranyaSetu now opens into the FRA workspace after sign-in, presents the `Ingest → Structure → Spatialize → Enrich → Visualize → Recommend` lifecycle, describes its FRA Spatial Intelligence and Decision Support purpose in OpenAPI and README content, and labels Patta tooling as cadastral evidence. Verified by the full Python suite (`362 passed`, `12 skipped`, `73 subtests`) and browser suite (`53 passed`) on 2026-09-05.
- **2. Fix the FRA data model:** Native claims now link directly to canonical FRA villages and may identify the claim they supersede. Typed decision date/reference, evidence page ranges, title granted area, database domain constraints, and field-level extraction review provenance were added through migration `20260905_0006`. Verified by SQLite/API/model/migration tests, the full Python suite (`366 passed`, `12 skipped`, `73 subtests`), the browser suite (`53 passed`), and native PostgreSQL/PostGIS tests (`182 passed`, `45 subtests`) on 2026-09-05.
- **3. Build the legacy-data ingestion pipeline:** Legacy intake now accepts scanned PDF/images and UTF-8 CSV/XLSX registers in the main FRA workspace. Register rows are header-mapped into the same review queue while the private original, row/header/raw value, extraction method, confidence, schema version, correction, and final approved value remain linked per field. The companion staged vector intake was verified with GeoJSON, real zipped Shapefile, a safe built-in KML reader, and real GeoPackage data; CRS/geometry validation, repair/duplicate provenance, malware checks, and reviewer publication remain enforced. Verified by the full Python suite (`373 passed`, `12 skipped`, `73 subtests`), browser suite (`54 passed`), and disposable native PostgreSQL/PostGIS suite (`200 passed`, `45 subtests`) on 2026-09-05.
- **4. Improve the OCR/NER layer:** Scanned images and PDF pages now pass through image preprocessing and page-level PaddleOCR while retaining per-line confidence and bounding boxes. The FRA extractor identifies Tamil/English rights holders, administrative units, survey/subdivision references, claim/right/status fields, decision authorities and dates, FRA title numbers, coordinates, and unit-backed land areas; conflicts and invalid values remain unset for correction. Extraction runs retain field evidence and privacy-safe page summaries, the review UI exposes uncertainty, and allow-listed REST model adapters must return reviewable field evidence. Sample-driven standalone Patta-number extraction was removed. Verified by the full Python suite (`380 passed`, `12 skipped`, `73 subtests`), browser suite (`55 passed`), and disposable native PostgreSQL/PostGIS suite (`198 passed`, `45 subtests`) on 2026-09-05.
- **5. Create a proper legacy-to-native FRA mapping:** Reviewed archive records now normalize claim type/status, areas, and decision dates; detect previously promoted duplicates; match canonical FRA villages and rights holders; resolve exact survey/subdivision parcels as supporting spatial evidence; and preserve source, reviewer, matching, and normalization provenance. Historical decisions and titles are reconstructed only from reviewed fields and remain labelled as legacy mappings. The older registry compatibility service now requires a reviewed FRA intake and cannot directly create a native case. Verified by the full Python suite (`382 passed`, `12 skipped`, `73 subtests`) and disposable native PostgreSQL/PostGIS suite (`200 passed`, `45 subtests`) on 2026-09-05.
- **6. Fix authorization and workflow controls:** Claim visibility is consistently owner-scoped with reviewer/admin oversight across foundation, case, asset, evidence, operations, planning, and report routes. Archive records and staged vector imports now follow the same owner/reviewer boundary, archive creation verifies batch/document ownership, and post-adjudication geometry versioning requires a reviewer. Extraction approval, legacy promotion, lifecycle decisions, title creation, reference publication, asset review, and referrals remain reviewer/admin actions; the service-level legacy bypass is closed. Verified by the full Python suite (`383 passed`, `12 skipped`, `73 subtests`) and disposable native PostgreSQL/PostGIS suite (`201 passed`, `45 subtests`) on 2026-09-05.
- **7. Build the spatial layer:** FRA claim boundaries accept validated Polygon/MultiPolygon input and are stored as versioned EPSG:4326 MultiPolygons; asset observations support native EPSG:4326 Point and MultiPolygon storage. Saved boundaries record computed area, linked-village containment, and right-aware overlap findings without turning spatial evidence into an automated legal result. Canonical village, claim, reference-feature, imagery-footprint, and asset geometry columns now have native GIST indexes through migration `20260905_0007`; titles remain linked to versioned claim geometry. Verified by the full Python suite (`384 passed`, `13 skipped`, `73 subtests`) and disposable native PostgreSQL/PostGIS suite (`203 passed`, `45 subtests`) on 2026-09-05.
- **8. Build the FRA Atlas:** The protected Atlas now combines published India/state/district/block administrative references, canonical villages, spatial IFR/CR/CFR claims and titles, satellite asset observations, and published forest, protected-area, cadastral, water, infrastructure, and forest-compartment references. One shared query applies location, right type, lifecycle status, tribal/community category, year, and claim/title area filters to the feature collection and summary; reference responses expose source provenance without private import properties. The UI groups FRA, administrative, and supporting layers while keeping cadastral data in its supporting role. Verified by the full Python suite (`385 passed`, `13 skipped`, `73 subtests`), browser suite (`55 passed`), and disposable native PostgreSQL/PostGIS suite (`203 passed`, `45 subtests`) on 2026-09-05.
- **9. Add FRA statistics to the Atlas:** Atlas statistics now count every structured FRA case in the selected scope, including cases awaiting geometry, and display total/IFR/CR/CFR, granted/rejected/pending, claimed area, and granted area from active titles. The API produces filter-aware progress rows at state, district, block, and village levels. Selecting a row updates the shared administrative context; authorized claim and title features open the existing case evidence and decision record. Canonical village links now drive claim location before legacy fallback locations. Verified by the full Python suite (`386 passed`, `13 skipped`, `73 subtests`), browser suite (`57 passed`), and disposable native PostgreSQL/PostGIS suite (`203 passed`, `45 subtests`) on 2026-09-06.
- **10. Implement real satellite-data ingestion:** Spatial FRA claims can now queue a bounded, idempotent Sentinel-2 ingestion job through the protected API and Asset Intelligence workspace. The worker performs allow-listed Earth Search STAC discovery, selects a cloud-filtered scene with required bands, window-reads public Cloud-Optimized GeoTIFFs, reprojects/aligns and clips them to the current claim boundary, and stores a private multi-band analysis-ready GeoTIFF with checksum, CRS, transform, valid-pixel, acquisition, collection, license, and processor provenance. Provider URLs and private storage keys remain redacted. A live Tamil Nadu smoke check found four Sentinel-2 L2A scenes and read a valid TIFF byte range from the selected COG; no manifest fallback is used by this flow. Verified by the full Python suite (`389 passed`, `13 skipped`, `73 subtests`), browser suite (`58 passed`), and disposable native PostgreSQL/PostGIS suite (`206 passed`, `45 subtests`) on 2026-09-06.
- **11. Real AI asset detection:** Deferred at the user's direction because a trained asset-detection model will be supplied separately. The Asset Intelligence workspace now shows an inactive attachment point describing the required model artifact, label map, preprocessing contract, and adapter configuration; it does not generate synthetic detections or claim that inference is available. Item 10's real analysis-ready raster producer remains the input boundary for that model.
- **12. Standardize the asset taxonomy:** Satellite observations, persisted asset features, protected APIs, Atlas metadata/features, planner aggregates, DSS fact derivation, reports, sample rules, and the Asset Intelligence UI now use taxonomy `fra-assets-v1`: `agricultural_land`, `water_body`, `homestead`, `forest_cover`, `road`, `infrastructure`, and `other_asset`. Legacy/model labels are normalized only at input boundaries and retained as `asset_subtype`; model registration validates and canonicalizes label maps. Migration `20260906_0008` converts existing rows and enforces both asset tables with database check constraints. Verified by the full Python suite (`402 passed`, `13 skipped`, `73 subtests`), browser suite (`58 passed`), and disposable native PostgreSQL/PostGIS suite (`208 passed`, `45 subtests`) on 2026-09-06.
- **13. Create village-level asset profiles:** Reviewers can rebuild one persisted `village-assets-v1` profile for every canonical FRA village through the protected asset API. Profiles calculate agricultural and forest area, water-body count/area, homestead count, road and infrastructure availability, other asset counts, mean confidence, and imagery dates from verified observations; pending observations remain a separate review count. Each profile retains a source digest, imagery reference, source type, model name/version, taxonomy/calculation versions, and generation audit, while API/report views redact private references. The Asset Intelligence workspace lists the scoped profile metrics, and the Tamil Nadu sample seed creates profiles idempotently. Migration `20260906_0009` adds the persisted profile table. Verified by the full Python suite (`404 passed`, `13 skipped`, `73 subtests`), browser suite (`59 passed`), and disposable native PostgreSQL/PostGIS suite (`209 passed`, `45 subtests`) on 2026-09-06.
- **14. Add imagery/reference layers:** The protected FRA Atlas now consumes completed current Sentinel-2 analysis-ready artifacts and available historical observations as claim-area imagery coverage layers with acquisition, collection, license, processor, quality, and verification metadata. Imagery year filters use the artifact/acquisition year independently of the claim year; private storage keys and provider asset URLs remain redacted, while authorized owners and reviewers can open the owning FRA evidence case. Published groundwater, groundwater-stress, and water-stress imports now flow through the existing staged import, Atlas, and spatial-intersection paths alongside forest, water, infrastructure, cadastral, and administrative references; groundwater findings remain contextual analytical evidence. Verified by the full Python suite (`406 passed`, `13 skipped`, `73 subtests`), browser suite (`60 passed`), and disposable native PostgreSQL/PostGIS suite (`209 passed`, `45 subtests`) on 2026-09-06.
- **15. Connect satellite assets to FRA villages:** Village profile rebuilds now resolve assets through explicit village targets, FRA claim targets, or spatial intersection when no target exists, with explicit links taking precedence to prevent duplicate assignment across overlapping boundaries. Persisted `village-assets-v2` metrics calculate verified satellite assets within village boundaries, intersecting current FRA land, within 1 km of FRA land, unioned polygon coverage, linked claim/title counts, and observation-gap indicators. An owner/reviewer-protected claim endpoint exposes the complete claim/title → village → asset relationship with per-asset relation and distance, FRA-area coverage, and no private imagery reference. All results are labelled supporting analytical information rather than legal findings. Verified by the full Python suite (`408 passed`, `13 skipped`, `73 subtests`), browser suite (`60 passed`), and disposable native PostgreSQL/PostGIS suite (`210 passed`, `45 subtests`) on 2026-09-06.
- **16. Fix the DSS data contract:** New derivations use the exact `fra-dss-facts-v1` contract, published through the protected API with declared types. The contract standardizes active title, water, homestead, agriculture presence/fraction, forest cover, groundwater/water stress, road access, infrastructure, village context, and FRA-linked asset coverage/deficiency facts; every fact retains an evidence envelope and source. New rule registrations reject unknown, duplicate, or undeclared condition facts, and derived evaluations revalidate stored rules before use. Migration `20260906_0010` maps known legacy names and deactivates rules with unmapped facts while preserving historical snapshots and recommendations. Verified by the full Python suite (`411 passed`, `13 skipped`, `73 subtests`), browser suite (`60 passed`), and disposable native PostgreSQL/PostGIS suite (`212 passed`, `45 subtests`) on 2026-09-06.
- **17. Build the DSS rule engine:** Scheme rules now persist and validate separate eligibility conditions, required evidence facts, required canonical asset classes, exclusion conditions, priority conditions, per-fact freshness windows, and outcome-specific recommendation logic. Evaluation distinguishes `recommended`, `not_recommended`, and `insufficient_data`; records unmet assets, stale or missing evidence, exclusion reasons, and the highest matched priority while retaining source versions and the advisory-only disclaimer. The versioned synthetic rules exercise all dimensions, and migration `20260906_0011` adds backward-compatible defaults for existing rules. Verified by the full Python suite (`412 passed`, `13 skipped`, `73 subtests`), browser suite (`60 passed`), and disposable native PostgreSQL/PostGIS suite (`213 passed`, `45 subtests`) on 2026-09-06.
- **18. Add scheme convergence:** The catalogue now carries structured, validated convergence scope, intervention types, FRA right applicability, prerequisites, and evidence facts for PM-KISAN, MGNREGA, PMAY-G, JJM, and DAJGUA while remaining explicitly draft, inactive, and non-authoritative. Candidate rule codes now align with those programmes. A protected claim/holder/village convergence API combines the latest recommendation with its catalogue entry and reports potentially eligible review candidates, rejections, insufficient data, unevaluated schemes, missing prerequisites, supporting evidence, priority, and reasons without representing an official eligibility or sanction decision. The existing planner list consumes this projection and preserves owner/reviewer case visibility. Verified by the full Python suite (`418 passed`, `13 skipped`, `75 subtests`), browser suite (`60 passed`), and disposable native PostgreSQL/PostGIS suite (`221 passed`, `47 subtests`) on 2026-09-06.
- **19. Complete water DSS use case:** A verified, coverage-qualified imagery assessment of absent water assets and overlapping published groundwater/water-stress references now travel through the persisted database records, `fra-dss-facts-v1` evidence envelopes, the versioned JJM candidate rule, an urgent advisory recommendation, the scheme-convergence projection, and the planner UI. The visible result names the recommendation reason and its imagery-artifact and spatial-reference evidence while retaining the draft catalogue status and no-eligibility disclaimer. Verified by a dedicated end-to-end journey test, the full Python suite (`419 passed`, `13 skipped`, `75 subtests`), browser suite (`61 passed`), and disposable native PostgreSQL/PostGIS suite (`222 passed`, `47 subtests`) on 2026-09-06.
- **20. Govern DSS scheme/rule integration:** Every newly registered rule must identify its exact `SchemeCatalogEntry`; executable rules are accepted only when the scheme code matches an active, authoritative catalogue version and the rule dates fit within the approved catalogue period. Runtime evaluation rechecks catalogue activation and dates, so withdrawing or expiring catalogue approval stops linked execution. Migration `20260906_0012` adds the foreign key, links compatible approved legacy rules, and deactivates unlinked ones. Bundled candidate rules remain linked but inactive while their catalogue entries are draft, and rule API/convergence output exposes the exact catalogue version. Verified by the full Python suite (`422 passed`, `13 skipped`, `75 subtests`), browser suite (`61 passed`), and disposable native PostgreSQL/PostGIS suite (`225 passed`, `47 subtests`) on 2026-09-06.
- **21. Build the DSS planner UI:** The planner now applies the shared state/district/block/village context and adds FRA-category, intervention-type, programme, and evaluation-result filters. Its convergence summary distinguishes potential candidates, not-indicated cases, insufficient data, and unevaluated programmes; each result presents FRA status, mapped assets, deficiencies, recommended interventions, evidence completeness and sources, unknown data, priority, prerequisites, catalogue governance, and advisory referral controls without implying official eligibility or sanction. Verified by focused Python/UI tests (`23 passed`, `5 subtests`), the full Python suite (`423 passed`, `13 skipped`, `75 subtests`), browser suite (`61 passed`), and disposable native PostgreSQL/PostGIS suite (`226 passed`, `47 subtests`) on 2026-09-06.
- **22. Build the end-to-end dashboard:** The hierarchy-filtered FRA Overview now has dedicated FRA, spatial, development, and DSS sections. It reports claims, titles, pending cases, lifecycle decisions, claimed and title-granted FRA area, villages covered, reviewed asset distribution, water/agriculture/homestead/infrastructure availability and deficiencies, recommended intervention counts, scheme convergence states, insufficient-data cases, missing inputs, referrals, and reviewer work queues. Area granted is derived from active FRA titles, and the shared claim-location resolver includes cases linked only through the rights holder's Gram Sabha. Output remains aggregate and privacy-minimized. Verified by focused service/API/UI tests (`53 passed`, `5 subtests`), the full Python suite (`423 passed`, `13 skipped`, `75 subtests`), browser suite (`62 passed`), and disposable native PostgreSQL/PostGIS suite (`226 passed`, `47 subtests`) on 2026-09-06.
- **23. Build evidence/provenance UI:** Reviewer archive details now expose a collapsible chain for each extracted FRA field: extracted value, source office, sanitized document identity, page or tabular row, extraction method/model/confidence, reviewer state and timestamp, correction, and final approved value. The source panel also identifies the document and source without returning its private storage key. Every satellite/field asset row exposes its asset class/subtype, sanitized imagery or source reference, acquisition date, registered model/version, confidence, geometry type, and verification state; private imagery references remain redacted. Verified by focused archive/asset API and UI tests (`25 passed`, `8 subtests`), browser-level rendering tests, the full Python suite (`423 passed`, `13 skipped`, `75 subtests`), browser suite (`66 passed`), and disposable native PostgreSQL/PostGIS suite (`226 passed`, `47 subtests`) on 2026-09-06.
- **24. Build the review interface:** The FRA archive workspace now supports OCR inspection, field correction, extraction approval, and reasoned extraction rejection with optimistic revision checks and an audit trail. Existing claim, evidence, and geometry review remain integrated in the FRA case workspace. Unverified asset detections now expose version-checked approve/reject controls backed by the protected review API, while DSS candidates can be sent to the responsible department for explicit human review without implying eligibility or sanction. Verified by focused archive/asset/API/UI tests (`41 passed`, `8 subtests`), the full Python suite (`425 passed`, `13 skipped`, `75 subtests`), browser suite (`67 passed`), and disposable native PostgreSQL/PostGIS suite (`228 passed`, `47 subtests`) on 2026-09-06.
- **25. Make background processing production-safe:** Processing jobs now use expiring ownership tokens, renewable heartbeats, PostgreSQL `SKIP LOCKED` dispatch, stale-worker fencing, automatic recovery of expired and pre-lease running jobs, bounded exponential retry delays, retained failure history, terminal failure/quarantine states, reviewer retry controls, and protected operational status fields. Migration `20260906_0013` adds the lease/history columns and dispatch indexes and requeues legacy stranded work during deployment. Compose now starts a separate continuous worker after API/migration readiness, with shared private storage and graceful signal handling; poll failures are reported without leaking exception text. Verified by focused worker/API/config/migration tests (`32 passed`, `4 subtests`), the full Python suite (`433 passed`, `14 skipped`, `75 subtests`), disposable native PostgreSQL/PostGIS tests (`243 passed`, `47 subtests`) including concurrent distinct job claims, Compose configuration validation, a successful worker image build, and a non-mutating container startup smoke check on 2026-09-06.
- **26. Stabilize the database/migrations:** Revisions 0001, 0003, 0004, and 0005 now contain frozen table/index/foreign-key/unique definitions and no longer create schema from live ORM metadata; their cross-dialect geometry type is migration-owned. The exclusive parcel rule remains in its chronological revision, PostGIS extension tables are excluded from application drift checks, geometry reflection is registered, and the parcel GIST and scheme-catalogue foreign-key indexes now agree with ORM metadata. Fresh SQLite schemas upgrade, downgrade to base, rebuild, and pass Alembic's zero-drift check; fresh PostGIS schemas also report no pending upgrade operations before the full native suite. Verified by migration tests (`9 passed`, `4 subtests`), the full Python suite (`437 passed`, `14 skipped`, `79 subtests`), native PostgreSQL/PostGIS tests (`243 passed`, `47 subtests`), and a fresh disposable database built and drift-checked inside the deployment image on 2026-09-06.
- **27. Add integration tests:** Six named end-to-end journeys now verify scanned PDF rendering and OCR adapter output through extraction review and native FRA promotion; legacy registry triage into a native case; reviewed claim geometry in the Atlas; a completed satellite artifact through the explicit user-model adapter boundary into a reviewed canonical asset and village profile; claim/title/asset evidence through the versioned fact contract and governed DSS rule; and the resulting advisory recommendation in both the planner projection and printable village report. The tests assert the producer-consumer contracts for `fra-assets-v1`, `fra-dss-facts-v1`, rule inputs/outputs, and exact scheme catalogue/rule versions. External OCR and the deferred user-supplied detector are deterministic test adapters only; PDF rendering, services, persistence, review, spatial calculations, and consumers remain real. Verified by the focused integration/report suite (`16 passed`), full Python suite (`443 passed`, `14 skipped`, `79 subtests`), and fresh disposable PostgreSQL/PostGIS suite (`249 passed`, `47 subtests`) with zero Alembic drift on 2026-09-06.
- **28. Validate with real data:** The repository now carries a small, public, non-synthetic Tamil Nadu validation bundle: the Survey of India polygon for Census village `632998` from the National Water Data Portal, the exact Tamil Nadu claims/title totals from page 7 of MoTA's June 2026 FRA progress report, and a sanitized live Earth Search Sentinel-2 L2A scene intersecting that village. The canonical village importer now accepts real boundaries only for administrators and only with explicit authority, public source, licence, version, and `published_authoritative_reference` classification. A reproducible validator checks count arithmetic, SHA-256 source identity, Tamil Nadu geometry validity, village/scene intersection, and absence of persisted provider asset URLs. The supplied real Patta scan was run through PaddleOCR (`88.53%` mean confidence, `52` lines); it correctly remains supporting cadastral evidence and is not promotable as an FRA record because required FRA fields are absent. The privacy-safe report stores no raw OCR text, local path, or personal field values. Claim-level Tamil Nadu FRA documents, individual title records, and legal claim/title geometries remain explicitly unavailable rather than being fabricated; public state aggregates are never expanded into synthetic people or cases. Verified by focused real-data/import/integration tests (`19 passed`), the full Python suite (`449 passed`, `14 skipped`, `79 subtests`), an actual OCR run on the supplied image, live STAC discovery, and a migrated native PostGIS authoritative-import check (`1 passed`, `256 deselected`) on 2026-09-06.
- **29. Complete Tamil Nadu pilot:** An idempotent Villupuram/Arpisampalaiyam pilot runner now exercises the authoritative Survey of India village import, a visibly synthetic FRA document and extraction/review fixture, legacy-to-native CFR promotion, a synthetic inset claim geometry linked to the real village, filtered FRA Atlas output, the real sanitized Sentinel-2 scene reference, an empty but versioned village asset profile, `fra-dss-facts-v1` derivation, and a governed JJM evidence-readiness result. The trained asset stage remains `awaiting_user_model`; no detections are fabricated, and the resulting scheme output is `insufficient_data` with the missing water observation named. The pilot uses an isolated pilot scheme code and does not activate or replace operational JJM configuration. A machine-readable stage report and provenance table document every real, synthetic, and deferred boundary. Verified by the focused pilot/Atlas/DSS suite (`38 passed`, `5 subtests`), the full Python suite (`451 passed`, `14 skipped`, `79 subtests`), and the disposable native PostgreSQL/PostGIS suite (`259 passed`, `47 subtests`) with zero Alembic drift on 2026-09-06.
- **30. Final UI hierarchy:** The protected workspace now opens on FRA Overview and exposes exactly six top-level destinations in the requested order: FRA Overview, FRA Atlas, Case Management, Asset Intelligence, DSS Planner, and Reports. Legacy FRA ingestion remains fully available inside Case Management alongside Native FRA Cases and Registry Intake, with stable `#cases/legacy` deep links and dashboard queue routing. Atlas, asset, DSS, and report panels explicitly name their requested maps, filters, claim/title/asset layers, satellite/review/profile functions, scheme/evidence-gap outputs, and FRA/spatial/asset/convergence report coverage. Verified by the full browser suite (`68 passed`), the full Python suite (`452 passed`, `14 skipped`, `95 subtests`), a rebuilt healthy Docker API/worker stack, and live browser checks of the Overview, Native FRA Cases, and Legacy Records views on 2026-09-06.
- **31. Reduce Patta prominence:** The retained Patta OCR, survey/subdivision matching, official parcel resolution, source-document review, and parcel registry now sit in a clearly subordinate **Cadastral Evidence** workspace for FRA spatialization. User-facing workflow language consistently says “Add evidence,” “parcel link,” and “Return to FRA Platform”; the page explains that these links support land identification and do not establish FRA rights or alter ownership. Compatibility routes remain stable while OpenAPI groups them as supporting cadastral evidence. Verified by the full Python suite (`454 passed`, `14 skipped`, `101 subtests`), browser suite (`68 passed`), a rebuilt healthy Docker API/worker stack, and a live authenticated browser check on 2026-09-06.
- **32. Remove or rename misleading branding:** Runtime, API, module, UI, README, and deployment terminology now consistently presents **AranyaSetu — FRA Spatial Intelligence + DSS**. The API catalogue leads with `/api/fra/document-intelligence/*` and `/api/cadastral-evidence/*`; old OCR/Patta/claim paths remain undocumented compatibility aliases. Ambiguous route, schema, service, static-workspace, and test modules were renamed around document intelligence, cadastral evidence, and parcel links. Compose now uses the `aranyasetu` project, `aranyasetu-api:local` and `aranyasetu-worker:local` images, and the AranyaSetu private-storage path; the old Compose volumes remain untouched. Verified by the full Python suite (`457 passed`, `14 skipped`, `101 subtests`), browser suite (`68 passed`), Compose rendering, a healthy rebuilt AranyaSetu stack, live preferred-route OpenAPI inspection, and an authenticated `/cadastral-evidence` browser check on 2026-09-06.
- **33. Update documentation:** [`ARCHITECTURE.md`](ARCHITECTURE.md) now documents the problem and scope, architecture, data model, legacy ingestion, OCR/NER, spatial design, real Sentinel-2 pipeline, deferred user asset-model boundary, `fra-assets-v1`, village profiles, FRA Atlas, `fra-dss-facts-v1`, governed scheme rules, authorization, provenance, deployment, and current limitations. README, FRA workflow, model adapter, privacy, operations, codebase-analysis, and pilot guides were reconciled with migration head `20260906_0013` and the final UI/API/deployment names. The unused OCR-service Word document and obsolete Patta-workflow screenshot were removed; the earlier cadastral plan is clearly archived. Verified by documentation regression tests, resolution of all current local Markdown links, `git diff --check`, and the full Python suite (`459 passed`, `14 skipped`, `101 subtests`) on 2026-09-06.
- **34. Update demonstration flow:** The idempotent Tamil Nadu pilot now emits an executable 17-step contract in the exact requested order, from legacy FRA intake through report generation. Each step includes its number, title, status, evidence, and limitation, while the report also returns stable village/case/scene/recommendation references and the generated village-report URL/SHA-256. Asset detection and mapped-asset display remain honestly `awaiting_user_model`; the empty profile and `insufficient_data` DSS result allow the remaining evidence/reasoning/report stages to proceed without fabricated detections. [`DEMO_FLOW.md`](DEMO_FLOW.md) maps every step to its workspace action and explains the post-model handoff. Verified by focused pilot/documentation tests, the full Python suite (`460 passed`, `14 skipped`, `101 subtests`), a live idempotent Docker run reporting ordered steps 1–17, and an authenticated browser rendering of the generated Arpisampalaiyam village report on 2026-09-06.
- **35. Final priority-order outcomes:** [`FINAL_OUTCOME_AUDIT.md`](FINAL_OUTCOME_AUDIT.md) accounts for all 11 P0, 9 P1, and 8 P2 outcomes without presenting external dependencies or future work as implemented. Every P0/P1 core result is linked to its implementation evidence; real Sentinel-2 ingestion is verified while trained asset inference remains the user-directed `awaiting_user_model` placeholder, and the Tamil Nadu pilot discloses its real public inputs and synthetic claim-level gaps. P2 historical analysis is implemented, reference/scheme expansion is partially available, and additional models, advanced AI recommendations, mobile feedback, IoT, and continuous satellite monitoring remain future scope. Final verification passed: full Python (`461 passed`, `14 skipped`, `101 subtests`), browser (`68 passed`), native PostgreSQL/PostGIS (`259 passed`, `47 subtests`) with zero Alembic drift, migration head `20260906_0013`, compileall, Compose/live readiness, current Markdown links, and `git diff --check` on 2026-09-06.
